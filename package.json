{
  "name": "monsoonmind-root",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "server": "npm run start --prefix server",
    "client": "npm run dev --prefix client",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0"
  }
}
